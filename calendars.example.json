{
  "_comment": "Per-calendar behavior overrides. Keys are calendar IDs (same as in CALENDAR_ID). Any option you omit falls back to _defaults below. Copy to calendars.json and edit.",
  "_defaults": {
    "notify": true,
    "autoOpen": true,
    "warnNoLink": true,
    "push": true
  },
  "robertjan.tuit@gmail.com": {
    "warnNoLink": false,
    "color": "red"
  },
  "rj@clickup.com": {
    "color": "yellow"
  }
}
